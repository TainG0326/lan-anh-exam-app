/**
 * Asset trong `public/` — encodeURI để tên có dấu/khoảng trắng ổn định trên mọi host.
 */
export const TEACHER_LOGO_SRC = encodeURI('/Học viện Anh ngữ Lan Anh.png');

export const TEACHER_LOGIN_BG_DESKTOP = encodeURI('/login-backround for desktop.png');

export const TEACHER_LOGIN_BG_MOBILE = encodeURI('/login-backround for mobile.png');
