# Environment Variables for Student Web

Create a `.env` file in this directory with:

```bash
# API URL - Change to your server URL in production
VITE_API_URL=http://localhost:5000/api
```

The application will use `http://localhost:5000/api` by default if no `.env` file is provided.



